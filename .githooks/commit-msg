#!/bin/sh
# Strip Cursor / AI co-author trailers from commit messages
if [ -f "$1" ]; then
  grep -v -i 'Co-authored-by: Cursor' "$1" | grep -v -i 'Made-with: Cursor' > "$1.tmp" && mv "$1.tmp" "$1"
fi
exit 0
